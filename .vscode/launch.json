{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Run OpenClawManager",
            "program": "${workspaceFolder}/.build/debug/OpenClawManager",
            "preLaunchTask": "swift-build"
        }
    ]
}
